export const APP_NAME = "Tigress";

/** Total number of billiards tables in the venue. */
export const TABLE_COUNT = 7;

/** Default booking slot length in minutes. */
export const DEFAULT_SLOT_MINUTES = 60;

/** Roles used across the app. */
export const ROLES = {
  MEMBER: "Member",
  STAFF: "Staff",
  MANAGER: "Manager",
  OWNER: "Owner",
} as const;
